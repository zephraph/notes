#work 
- Fixed a bug in production where the `/auctions/upcoming` tab was throwing an error due to a `sale_artwork` being undefined in the `Details` component. This would've been caught if we had strict null checking.