## Issues to fix

- Input field doesn't detect error until first tab away when we could be detecting as the user types. This is specifically for things like invalid names. See `Choose a name` in project create as an example. 